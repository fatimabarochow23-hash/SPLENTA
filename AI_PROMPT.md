🚀 任务：实现专业级动态包网络可视化视图 (Enforcer 风格) - 更新版本（2025 11.27）
Claude，请仔细阅读这份文档更新后的指令。这份文档包含了对您之前问题的解答，以及新增需求的实现路径和技术决策。工程路径：/Users/MediaStorm/Desktop/NewProject/Source/￼1. 针对首次提问的回答与确认我们确认了技术路线，请基于以下回答开始执行任务：1. 是否完全替换RollingScope？• 回答：是的，完全替换。EnvelopeView将替换原来左侧的波形显示组件( RollingScope)。请在PluginEditor.h/cpp中删除旧组件并集成新组件。2. DSP信号连接点确认：• 重要前提：所有用于可视化的信号必须是线性幅度值(Linear Amplitude, 0.0f 到 ~1.0f+)，而不是分贝 (dB) 值。• Detector envelope: 确认连接到detectorEnv(PluginProcessor.cpp中计算的)。• Synthesizer envelope：连接到envAmplitude。我们需要可视化的是合成器生成的幅度包络控制信号。• Output envelope: 是的，这是最终输出信号（湿/干混合后）的包络。你必须添加逻辑来计算最终输出缓冲器的热点包络。请在的构成中processBlock，针对最终的输出buffer计算每个样本的幅度（例如使用std::abs(sampleValue)），放入其升温逻辑。3. 是否开始实现？• 回答：是的。请立即开始实现下列所有需求。￼2. 核心需求增益与架构要求请确保以下核心架构要求得到满足：• 数据传输：使用juce::AbstractFifo实现无锁通信（音频线程 -> UI 线程）。• 峰值聚合（Peak Aggregation）：在processBlock中，每64个样本进行一次峰值聚合降采样（EnvUpdateRate = 64）。只进行一次峰值聚合后的峰值。• 高效滚动（Efficient Scrolling）：在EnvelopeView::timerCallback（60Hz）中使用std::rotate更新历史瀑布（historyBuffer）。禁止在时钟回调中进行内存分配。• 历史得分：HistorySize固定为512点。• 对数缩放（关键）：Y 轴必须使用对数映射，以显示约 60dB 的动态范围。• 公式：y = log(1 + k * value) / log(1 + k)(k=1000)。• 性能要求：必须实现一个高效的mapToLogScale函数，预先计算分母1 / log(1 + k)（例如使用static const float invLog1pk = ...;），避免在paint循环中重复计算。• 相似与风格 (ARC Rust 主题)：• 使用juce::Path，抗锯齿渲染 ( PathStrokeType::curved)。• 背景：#1A0E0A• 探测器（粗，细线）：#CC7733• 合成器（中层，焦点）：#FF8C42（带半透明填充Alpha 0.4 + 语音描边）• 输出（很，细线）：#FFD66B￼3.新增需求与技术指导【3.1】关于“释放值影响横向缩放”的技术说明我们希望实现的效果是：发布时长，横向内容更丰富（波形更密集）；发布时短，内容稀疏（波形更稀疏）。技术决策：采用固定时间窗口。• 我们决定不采用动态改变时间窗口（缩放）的方案。专业插件通常使用固定的时间窗口，以便用户有稳定的时间参考。• 实现方式：你消耗在EnvelopeView中做额外的工作。我们当前的设计（固定时间窗口）已经能自然实现这个效果：• 当释放值改变时，它会直接影响PluginProcessor中计算的包络形状。• 释放长 -> 包络衰减慢 -> 波峰变宽 -> 在固定时间窗口内看起来“内容丰富/密集”。• 释放 -> 包络衰减快 -> 波峰变窄 -> 在固定时间窗口内看上去“稀疏”。• 任务：请严格保留HistorySize(512点)和EnvUpdateRate(64样本/点)为固定值。【3.2】 较差阈值和上限参考线必须在EnvelopeView中势水平线上，代表当前的Threshold和Ceiling值。这条多余线必须与波形在视觉上准确对应。实现步骤(在EnvelopeView.cpp::paint中):1. 获取参数值(dB)：从processor(通过APVTS)获取Threshold和Ceiling当前值。确认这些值是分贝(dB)。你需要查找正确的参数ID。C++￼// 伪代码 (请替换为实际的参数 ID)
float thresholdDB = processor.getAPVTS().getRawParameterValue("THRESH_ID")->load();
float ceilingDB = processor.getAPVTS().getRawParameterValue("CEILING_ID")->load();
2. 转换为线性幅度：将dB值转换为线性幅度。这是因为我们可视化的包络就是线性幅度。C++￼float thresholdLinear = juce::Decibels::decibelsToGain(thresholdDB);
float ceilingLinear = juce::Decibels::decibelsToGain(ceilingDB);
3. 应用对数映射 (⚠️ 核心步骤)：使用高精度的mapToLogScale函数，将线性幅度值映射到可视化坐标系。C++￼float thresholdVisual = mapToLogScale(thresholdLinear);
float ceilingVisual = mapToLogScale(ceilingLinear);
4. 计算Y坐标：C++￼const float height = (float)getHeight();
float thresholdY = height * (1.0f - thresholdVisual);
float ceilingY = height * (1.0f - ceilingVisual);
5. 单色线与标签：在单色仪之后（确保在最准确），单色线。参考原始设计的颜色（淡青色/蒂芙尼蓝）。C++￼// 定义颜色 (参考截图中的青色)
const auto referenceColour = juce::Colour(0xFFA0E6E3); // 淡青色

// Ceiling (使用实线，更亮)
g.setColour(referenceColour);
g.drawLine(0.0f, ceilingY, (float)getWidth(), ceilingY, 1.5f);
// 绘制 'C' 标签 (请根据实际布局调整位置)
g.setFont(12.0f);
g.drawText("C", 5, (int)ceilingY - 14, 20, 12, juce::Justification::left, false);


// Threshold (使用虚线)
g.setColour(referenceColour.withAlpha(0.7f));
juce::Line<float> threshLine(0.0f, thresholdY, (float)getWidth(), thresholdY);
float dashLengths[] = { 4.0f, 4.0f };
g.drawDashedLine(threshLine, dashLengths, 2, 1.0f);
// 绘制 'T' 标签
g.drawText("T", 5, (int)thresholdY + 2, 20, 12, juce::Justification::left, false);
￼4. 执行要求请您现在开始根据文档更新后的文档，自动修改本地工程并生成全部代码。重点关注：1. DSP信号连接的精度（必须是线性幅度值，特别是Output Envelope的计算实现）。2. Threshold/Ceiling 线的映射逻辑（dB -> 必须 Linear -> Log Scale）精确实现。3. 严格遵守性能要求（高峰聚合、、std::rotate高效的mapToLogScale）。请直接修补所有文件。

20251127.02 更新提示词如下
# 🚀 任务：SPLENTA 视觉与交互重构 (V19.0 - Sniper & Cinematic Update)

Claude，我是技术负责人。这是 V19.0 的最终执行指令。
工程路径：/Users/MediaStorm/Desktop/NewProject/Source/￼

## 🎯 目标概述
我们需要解决 UI 割裂、滚动过快、交互困难等问题，并引入独特的 "Sniper Scope" 视觉反馈。
**核心原则：** 专业的视觉美感、丝滑的动画物理、直观的参数反馈。

---

## 1. 视觉与主题统一 (Theme Integration)
**问题：** `EnvelopeView` 的颜色目前是硬编码的，不跟随主界面的 Theme 切换。
**执行：**
1.  **修改 `EnvelopeView.h/cpp`：** 添加 `setThemeColors(Colour bg, Colour detector, Colour synth, Colour output, Colour grid)` 方法。
2.  **修改 `PluginEditor.cpp`：** 在 `updateColors()` 方法中，获取当前主题的配色，并调用 `envelopeView.setThemeColors(...)`。
    * *规则：* Synth 颜色必须与主色调 (Accent Color) 一致。Detector 和 Output 颜色根据 Accent 计算互补色或同色系变体。
    * *背景：* 必须完全透明或与 `PluginEditor` 背景融合，消除割裂感。

## 2. 滚动物理优化 (Cinematic Scroll)
**问题：** 当前滚动速度极快（产生动态模糊），且音频停止后波形不会消失（不归零）。
**执行：**
1.  **降采样率调整 (PluginProcessor.h)：**
    * 将 `envUpdateRate` 从 `64` 增加到 **`256`**。
    * *原理：* 48kHz / 256 ≈ 187Hz 数据率。在 60Hz UI 刷新下，每帧移动约 3 像素。这将产生缓慢、电影感的滚动效果，而不是现在的“飞速”效果。
2.  **淡出逻辑 (EnvelopeView.cpp)：**
    * 在 `timerCallback` 中添加“信号衰减”逻辑。
    * **逻辑：** 如果 FIFO 中没有新数据（或数据极小），且未处于 Freeze 状态，则对 `historyBuffer` 中的所有点应用衰减系数（例如 `val *= 0.96f`）。
    * *效果：* 声音停止后，波形会优雅地向下滑落/淡出，而不是僵死在屏幕上。
3.  **Freeze 功能：**
    * 在 UI 上添加一个小的 `[F]` (Freeze) 按钮。当激活时，暂停 `timerCallback` 中的历史数据更新和衰减逻辑。

## 3. UI 交互组件重构
**问题：** 扩展按钮太小；参考线 (T/C) 丑陋；缺少专业感。
**执行：**
1.  **圆形扩展按钮 (PluginEditor.cpp)：**
    * 重绘 `expandButton`。使用 `drawEllipse` 绘制圆形背景。
    * 在圆圈中心绘制加粗的箭头（`juce::Path`）。
    * *尺寸：* 至少 24x24 像素，确保容易点击。
2.  **参考线美化 (EnvelopeView.cpp)：**
    * **移除虚线：** Threshold 和 Ceiling 线全部改为**细实线** (1.0f)。
    * **移除梯形标签：** 删除 C/T 字母周围的梯形/方框背景。
    * **样式：** 仅在左侧边缘显示极小的、精致的文字 "THR" 和 "CEIL" (字体大小 10px, Opacity 0.6)。
    * **颜色：** 使用半透明的白色或灰色，不要使用扎眼的青色，除非它是当前 Theme 的强调色。

## 4. 新功能：狙击镜 (Sniper Scope)
**需求：** 当 FFT 被折叠（EnvelopeView 占据全宽）时，显示 Release 参数的可视化。
**实现逻辑 (EnvelopeView.cpp::paint)：**
1.  **条件：** 仅当 `!showFFT` (即 View 宽度较大) 时绘制。
2.  **形态：** 绘制一对括弧 `(   )` 位于视图中心或 Trigger 线附近。
    * 使用 `juce::Path` 绘制平滑的曲线括弧。
    * **风格：** 类似相机的对焦框或狙击镜刻度。
3.  **动态响应：**
    * 获取 `DET_REL` (Release) 参数值。
    * **映射：** `gapWidth = map(releaseMs, minMs, maxMs, minPx, maxPx)`。
    * *Min:* 即使 Release 最小，括弧也不重叠，形成一个近似圆环 `()`。
    * *Max:* Release 最大时，括弧展开，占据约 1/3 屏幕宽度。
4.  **交互：** 调节 Release 旋钮时，用户能直观看到括弧的开合动画。

## 5. DSP 信号处理修正
**问题：** Soft Clip 逻辑软绵；Auto-Gain 逻辑导致音量过大。
**执行 (PluginProcessor.cpp)：**
1.  **Soft Clip 改为 Hard Limiter：**
    * 废弃 `std::tanh`。
    * **实现 Pro-L2 风格限制：**
        ```cpp
        // 硬限制在 -0.01dB
        float limit = juce::Decibels::decibelsToGain(-0.01f);
        if (mixed > limit) mixed = limit;
        else if (mixed < -limit) mixed = -limit;
        // 简单的自动补偿 (可选，如果用户觉得声音变小了)
        // 但目前先保持纯粹的 Limiter 以防止过载。
        ```
2.  **Auto-Gain 修正：**
    * 现在的逻辑是 `InputRMS / OutputRMS`。如果 Output 很小（比如 Ducking 导致），Gain 会猛增。
    * **修正：** 添加保护限制。最大增益补偿不超过 +6dB。如果 OutputRMS < -60dB，不进行增益计算（防止除以零或噪音爆炸）。

---

## 🛠️ 文件修改清单
请严格按照以下步骤修改代码：

1.  **PluginProcessor.h/cpp:**
    * 修改 `envUpdateRate` 为 256。
    * 重写 `processBlock` 中的 Soft Clip 逻辑 (Hard Limit)。
    * 修正 AGM (Auto Gain) 的安全阈值。

2.  **EnvelopeView.h/cpp:**
    * 添加 `setThemeColors(...)`。
    * 添加 `drawSniperScope(...)` 私有方法。
    * 在 `timerCallback` 中实现数据衰减 (Decay)。
    * 在 `paint` 中重绘 T/C 参考线 (简约风格)。
    * 在 `paint` 中根据 `showFFT` 状态决定是否绘制 Sniper Scope。

3.  **PluginEditor.h/cpp:**
    * 重绘 `expandButton` (圆形 + 箭头)。
    * 在 `updateColors` 中将颜色传递给 `envelopeView`。
    * 添加 Freeze 按钮 (可选，如果空间不够优先保证扩展按钮易用性)。

## 🚀 执行指令
请读取当前所有代码，根据上述 V19.0 规范进行重构。保持代码整洁，**不要**解释每一行修改，直接输出修改后的完整文件代码块。优先处理 EnvelopeView 的视觉大修。
