你是 JUCE/C++ GUI 工程师。现在对 SPLENTA (V18.6 - 20251215.04) 执行 Batch 03（轻量 UI 重排 + 删除 FFT/折叠 + 旋钮值仅交互时显示）。必须最小改动，不碰音频逻辑、APVTS 参数定义，不破坏 Batch 02 的 StealthLookAndFeel。

你已拥有这些文件内容：
- Source/PluginEditor.h
- Source/PluginEditor.cpp
- Source/EnvelopeView.h/.cpp
- Source/StealthLookAndFeel.h/.cpp

========================
目标 1：顶部可视化区域重排
========================
现状：
- 顶部区域 topY=30, height=200
- 左侧 EnvelopeView + 右侧 FFT，通过 splitRatio/expandButton/divider 拆分

要求：
1) “示波器/EnvelopeView 的框”宽度缩小为当前的约 1/2（以宽度为准：原来总宽 940，现 envelopeArea 约 470 左右）。
2) 右侧剩余区域作为 “Energy Topology” 面板占位（本批只要容器/面板，不实现 Mobius 粒子算法）。

实现要点（必须做）：
- 在 PluginEditor 中新增 topologyArea: juce::Rectangle<int>（或新增一个可见 Component 作为占位容器）。
- 在 paint() 里像 envelopeArea 一样绘制 topologyArea 的 panel 背景与边框（使用 palette.panel900 填充，palette.accent.withAlpha(0.2f) 画边框）。
- topologyArea 里画一个标题文本，比如 "ENERGY TOPOLOGY"（字体小一些，alpha 0.6），位置左上角即可。

========================
目标 2：彻底砍掉 FFT 显示与折叠功能
========================
现状（必须移除的点）：
- PluginEditor.h: expandButton, showFFT, splitRatio, envelopeArea/fftArea/dividerArea, splitter dragging enum/currentDragAction
- PluginEditor.cpp: expandButton.onClick 切换 showFFT + isDynamicZoomActive
- mouseMove/mouseDown/mouseDrag/mouseUp 里关于 dividerArea/splitRatio 的逻辑
- timerCallback() 里对 nextFFTBlockReady 的 FFT transform 和绘制触发
- paint() 里 FFT 的整段绘制（mapFreqToX、网格线、fftPath、divider、箭头按钮绘制等）
- resized() 里 expandButton.setBounds(...) 以及任何依赖 splitRatio 的布局

要求：
- UI 上不再出现 FFT 区域、不再出现折叠按钮、不再出现分割线拖拽交互。
- 不要删除 APVTS 参数（如果存在 FFT 相关参数），但 UI 与 UI 定时更新必须不再驱动 FFT 计算与绘制。

========================
目标 3：旋钮数值“仅点击/拖拽时显示”，松开后隐藏（无布局跳动 + 淡入淡出）
========================
现状：
- setupKnob() 对每个 Slider 使用 TextBoxBelow（70x16），所以布局稳定，但目前一直可见（textBoxTextColourId 已设置为 white alpha 0.9）。

要求：
- 数值平时不可见（alpha=0），仅当该 slider 被按下/拖拽时显示（alpha=1）。
- 不产生布局跳动：必须继续保留 TextBoxBelow 占位（不要改成条件创建组件）。
- 有淡入淡出：alpha 变化不要瞬间跳变，做一个简易插值即可。
- 显示颜色使用主题 accent（来自 ThemePalette），不是固定白色。

推荐实现（按你判断选其一，但必须满足要求）：
方案 A（推荐，改动最小）：
1) setupKnob() 里仍然 slider.setTextBoxStyle(TextBoxBelow, ...)；
2) setupKnob() 末尾把 slider 文本框初始 alpha 设为 0：
   if (auto* tb = slider.getTextBox()) tb->setAlpha(0.0f);
3) updateColors() 里把每个 slider 的 textBoxTextColourId 设置为 palette.accent（替换目前 white）；
4) timerCallback() 里，每帧根据 slider.isMouseButtonDown() 决定目标 alpha：
   target = slider.isMouseButtonDown() ? 1.0f : 0.0f
   current = tb->getAlpha()
   tb->setAlpha(current + (target - current) * 0.2f)  // 简单淡入淡出
   备注：只对旋钮（Rotary）做即可。

注意：
- 不要用 hover 来显示值（hover 只用于 Batch02 的 glow），这里必须是 click/drag（mouseButtonDown）才显示。
- 不要改 APVTS 绑定和参数 suffix（slider.setTextValueSuffix 保留）。

========================
输出要求
========================
1) 先输出“修改/删除/新增点”文件清单（只涉及 PluginEditor.h/.cpp，必要时可新增一个简单占位组件类，但尽量不新增文件）。
2) 然后输出修改后的完整文件：
- Source/PluginEditor.h（全量）
- Source/PluginEditor.cpp（全量）
3) 不要改 EnvelopeView 与 StealthLookAndFeel（除非为配合数值颜色必须改极小处；原则上不改）。
4) 验收标准：
- 顶部左侧 EnvelopeView 面板宽度明显缩小到约一半
- 顶部右侧出现 Energy Topology 面板占位（有边框/标题）
- FFT/折叠按钮/分割拖拽完全消失，timer 不再做 FFT transform
- 旋钮值只有按下/拖拽时显示，松开淡出，布局不跳

现在开始输出修改后的完整 PluginEditor.h / PluginEditor.cpp。
